<UserControl x:Class="VersionManagement.Views.InputViews.DefinedSelectionSoftwareListsInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VersionManagement.Views.InputViews"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <GroupBox Header="{Binding Header}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!--<Button Content="-" Command="{Binding DeleteCommand}" ToolTip="Delete Item" HorizontalAlignment="Right" Height="20" Width="20"/>-->
                    <Button Content="+" Command="{Binding AddCommand}" ToolTip="Add Item" HorizontalAlignment="Right" Height="20" Width="20"/>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding ItemListFilter}"/>
            </StackPanel>
        </ScrollViewer>
    </GroupBox>
</UserControl>
